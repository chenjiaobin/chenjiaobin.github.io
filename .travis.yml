language: node_js
node_js: stable
# S: Build Lifecycle
install:
- npm install
before_install:
- git submodule update --init --remote --recursive
#before_script:
# - npm install -g gulp
script:
- hexo g
after_script:
- cd ./public
- git init
- git config user.name "chenjiaobin"
- git config user.email "1066628577@qq.com"
- git add .
- git commit -m "Update docs"
- git push --force --quiet "https://${Github_username}:${Github_password}@${GH_REF}" master:master
# E: Build LifeCycle
branches:
only:
- Source
env:
global:
- GH_REF: github.com/chenjiaobin/chenjiaobin.github.io.git